# @package _group_

defaults:
  - pretraining_shipsear_uteat

common:
  tensorboard_logdir: /hy-tmp/exp/eat/logs/deepship_uteat_formal_pretrain/tb

checkpoint:
  save_dir: /hy-tmp/exp/eat/runs/deepship_uteat_formal_pretrain

task:
  data: /hy-tmp/exp/eat/manifests/deepship

dataset:
  disable_validation: false

optimization:
  max_update: 10000

optimizer:
  groups:
    default:
      lr_scheduler:
        warmup_updates: 1000
